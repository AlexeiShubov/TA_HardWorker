using AxGrid.Model;
using ClassesTools;
using UnityEngine;

namespace UniRxTask
{
    public class DynamicCollectionTaskUniRx : DynamicCollection
    {
        /*[Bind]
        private void OnSwapCollectionObject(CollectionObject collectionObject)
        {
            Debug.LogError("+++++++++++++++");
            if (_activeCollectionPrefabs.Contains(collectionObject))
            {
                _activeCollectionPrefabs.Remove(collectionObject);
            }
            else
            {
                _activeCollectionPrefabs.Add(collectionObject);
            }
        }*/
    }
}